---
description: The rules for the React and TipTap reader window. Covers chapter documents, paragraph anchors, highlights, dialogs and the keyboard, memoization, settings contexts, and what the window may believe from the backend. Use when writing or changing anything under apps/desktop/src.
paths: apps/desktop/src/**
---

# Rules for the reader window

**Read [`docs/rules/frontend.md`](../../../docs/rules/frontend.md) and follow every rule in it.**

The rules live there, in plain Markdown, because this repository is agent-neutral: `AGENTS.md` is
canonical and every agent is pointed at the same page. This file only tells Claude Code when to open
it, so nothing is hidden behind one vendor's folder and there is one copy of each rule.
